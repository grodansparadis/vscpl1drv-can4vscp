#!/usr/bin/make -f

%:
	dh $@ --with autoreconf

override_dh_auto_install:
	strip linux/vscpl1drv-can4vscp.so
	install -D -m 0755 linux/vscpl1drv-can4vscp.so $$(pwd)/debian/vscpl1drv-can4vscp/srv/vscp/drivers/level1/vscpl1drv-can4vscp.so
