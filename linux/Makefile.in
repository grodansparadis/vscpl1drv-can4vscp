# =========================================================================
#                      Autogenerated from Makefile.in
# =========================================================================

# Package version
MAJOR_VERSION=@MAJOR_VERSION@
MINOR_VERSION=@MINOR_VERSION@
RELEASE_VERSION=@RELEASE_VERSION@
BUILD_VERSION=@BUILD_VERSION@
PACKAGE_VERSION=@PACKAGE_VERSION@

# STATIC = yes if a static build is requested.
STATIC=@STATIC@
VSCP_PROJ_BASE_DIR=@DESTDIR@
DESTDIR=@DESTDIR@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_builddir = @top_builddir@

CC = @CC@
CXX = @CXX@

WARNINGS_HARD = -Werror -Wall -Wextra -pedantic -Wcast-align -Wcast-qual \
  -Wctor-dtor-privacy -Wdisabled-optimization -Wformat=2 -Winit-self \
  -Wlogical-op -Wmissing-include-dirs -Wnoexcept -Wold-style-cast \
  -Woverloaded-virtual -Wredundant-decls -Wshadow -Wsign-promo \
  -Wstrict-null-sentinel -Wstrict-overflow=5 -Wundef -Wno-unused \
  -Wno-variadic-macros -Wno-parentheses -fdiagnostics-show-option
WARNINGS_MEDIUM = -Wformat-security -Wformat=2 -Wmissing-include-dirs \
  -Wno-parentheses -Wformat=2 -Wlogical-op
WARNINGS = -Wall

VERSION_DEFS = -DMAJOR_VERSION=$(MAJOR_VERSION) -DMINOR_VERSION=$(MINOR_VERSION) \
	-DRELEASE_VERSION=$(RELEASE_VERSION) -DBUILD_VERSION=$(BUILD_VERSION)

CFLAGS = -std=c99 @CFLAGS@ ${WARNINGS} -fPIC  -DCBC \
	-I../vscp/src/common \
	-I../vscp/src/vscp/common
CXXFLAGS = -D__LINUX__ -fPIC ${WARNINGS} -D_REENTRANT
CPPFLAGS = -D__LINUX__ -fPIC ${WARNINGS} -fno-var-tracking-assignments \
	-I../vscp/src/common \
	-I../vscp/src/vscp/common \
	$(VERSION_DEFS)

LDFLAGS = @LDFLAGS@
LIBS = @LIBS@
EXTRALIBS = @EXTRALIBS@

DLFLAGS = -g -shared -fPIC -Wl,--no-undefined
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib

LIB_PLUS_VER = "vscpl1drv-can4vscp.so.$(MAJOR_VERSION).$(MINOR_VERSION).$(RELEASE_VERSION)"
LIB_SONAME = "vscpl1drv-can4vscp.so.$(MAJOR_VERSION)"

### Variables: ###

CAN4VSCP_OBJECTS = vscpl1drv-can4vscp.o\
	can4vscpobj.o\
	dllist.o\
	crc8.o\
	com.o

### Targets: ###

all: $(LIB_PLUS_VER) ../man/vscpl1drv-can4vscp.1

test:  test.o $(CAN4VSCP_OBJECTS)
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -o $@ test.o $(CAN4VSCP_OBJECTS) @LIBS@ $(EXTRALIBS)

$(LIB_PLUS_VER): $(CAN4VSCP_OBJECTS)
	$(CXX) -Wl,-soname,$(LIB_SONAME) -o $@ $(CAN4VSCP_OBJECTS) $(DLFLAGS) $(LIBS) $(EXTRALIBS)
	ar rcs libvscpl1drv-can4vscp.a $(CAN4VSCP_OBJECTS)

can4vscpobj.o: ../common/can4vscpobj.cpp ../common/can4vscpobj.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../common/can4vscpobj.cpp -o $@

guid.o: ../vscp/src/vscp/common/guid.cpp ../vscp/src/vscp/common/guid.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../vscp/src/vscp/common/guid.cpp -o $@

dllist.o: ../vscp/src/common/dllist.c ../vscp/src/common/dllist.h
	$(CC) $(CFLAGS) -c ../vscp/src/common/dllist.c -o $@

crc8.o: ../vscp/src/common/crc8.c ../vscp/src/common/crc8.h
	$(CC) $(CFLAGS) -c ../vscp/src/common/crc8.c -o $@

com.o: ../vscp/src/common/com.cpp ../vscp/src/common/com.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../vscp/src/common/com.cpp -o $@

../man/vscpl1drv-can4vscp.1: ../man/vscpl1drv-can4vscp.1.md
	pandoc -s -t man ../man/vscpl1drv-can4vscp.1.md -o ../man/vscpl1drv-can4vscp.1

install: all
	$(INSTALL_DIR) $(DESTDIR)$(libdir)
	$(STRIP) $(LIB_PLUS_VER)
	$(INSTALL_PROGRAM) $(LIB_PLUS_VER) $(DESTDIR)$(libdir)
	ln -fs $(libdir)/$(LIB_PLUS_VER) $(DESTDIR)$(libdir)/vscpl1drv-can4vscp.so
	ln -fs $(libdir)/$(LIB_PLUS_VER) $(DESTDIR)$(libdir)/vscpl1drv-can4vscp.so.$(MAJOR_VERSION)
	ln -fs $(libdir)/$(LIB_PLUS_VER) $(DESTDIR)$(libdir)/vscpl1drv-can4vscp.so.$(MAJOR_VERSION).$(MINOR_VERSION
	$(INSTALL_PROGRAM) vscpl2drv-can4vscp.a $(DESTDIR)$(libdir)

man:
	pandoc -s -t man ../man/vscpl1drv-can4vscp.1.md -o ../man/vscpl1drv-can4vscp.1

uninstall: all
	rm -rf $(VSCP_PROJ_BASE_DIR)/drivers/level2/vscpl1drv-can4vscp.so

install-strip: install

deb:    all
	echo "\n" | ./builddist.sh
	@mkdir -p ../dist
	@mv *.deb ../dist

clean:
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../vscp/src/vscp/common/*.o
	rm -f ../vscp/src/common/*.o
	rm -f vscpl1drv-can4vscp.so
	rm -f test
	rm -rf ../common/*~
	rm -rf *~
	rm -f *.gz
	rm -f *.deb

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh

$(ALWAYS_BUILD):  .FORCE

.FORCE:

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE
